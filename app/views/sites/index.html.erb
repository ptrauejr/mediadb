<h1>Sites</h1>

<table>

  <thead>
    <tr>
      <th>Name</th>
      <th>URL</th>
      <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <% @sites.each do |site| %>
      <tr>
        <td><%= site.name %></td>
        <td><%= site.url %></td>
        <td><%= site.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>
